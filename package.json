{
  "name": "opvolging-it",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute opvolging-it-db --file=./migrations/001_init.sql",
    "db:migrate:remote": "wrangler d1 execute opvolging-it-db --remote --file=./migrations/001_init.sql"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
